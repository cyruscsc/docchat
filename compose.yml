services:
  docchat:
    build: .
    container_name: docchat-app
    restart: unless-stopped
    ports:
      - "8501:8501"
    environment:
      - STREAMLIT_SERVER_MAX_UPLOAD_SIZE=10 # Matching config.yaml
    volumes:
      - .:/app
